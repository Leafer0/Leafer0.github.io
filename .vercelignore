# ============================================================
#  Vercel 部署忽略清单
# ============================================================
#  作用：这些文件不会被上传到 Vercel。
#
#  为什么需要它：
#    页面实际只请求 img/、music/、vendor/、admin/ 和根目录的几个文件。
#    而 assets/（约 85MB）与 me/（约 12MB）是**原始大图的备份**，
#    页面从来不请求它们 —— 保留在仓库里是为了能随时换回原图。
#    如果把它们一起上传，每次部署都要多传 ~97MB，纯属浪费。
#
#  注意：被忽略的文件**仍然留在 Git 仓库里**，只是不参与 Vercel 部署。
#        所以"可回滚"这个特性不受影响。
# ============================================================

# 原始大图备份（页面不请求，见上）
assets/
me/

# 仓库里未被引用的历史图片（同样只是占体积）
# 如果想要它们也能在 Vercel 上访问，删掉下面这行即可
# （目前它们本来就没被任何页面引用）

# 开发与工具脚本：构建时不需要，运行时也不需要
tools/
node_modules/
package.json
package-lock.json
tailwind.config.js
src/

# Git 与 CI 配置
.github/
.gitignore

# 项目文档与本地配置
README.md
.git/

# 本地预览产生的截图
.preview/
.preview-live/
